<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- SQL语句 mapper标签可以有多个。但是namespace不能重 -->

<mapper namespace="cn.smbms.dao.user.MySqlTest">

	<select id="count" resultType="int">
		select count(1) from t_student
	</select>

	<!-- <select id="countname">
		select count(1) from t_student where name='张山'
	</select> -->

</mapper>